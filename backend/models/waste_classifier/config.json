{
  "architecture": "ResNet-18",
  "backbone": "torchvision.models.resnet18 (ImageNet pretrained)",
  "fine_tuned_weights": "resnet18_waste.pth (optional, auto-loaded if present)",
  "num_classes": 7,
  "input_size": [224, 224],
  "input_channels": 3,
  "class_names": [
    "battery",
    "biological",
    "cardboard",
    "glass",
    "metal",
    "paper",
    "plastic"
  ],
  "normalization": {
    "mean": [0.485, 0.456, 0.406],
    "std": [0.229, 0.224, 0.225]
  },
  "notes": "ImageNet normalization used as required by ResNet-18. Backbone weights are pretrained on ImageNet-1K. Final FC layer outputs 7 waste classes."
}